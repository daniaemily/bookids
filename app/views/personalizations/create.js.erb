var linksDiv = $('#personalization-links');

var htmlLinks = "<%= j link_to 'Preview your book', personalization_path(@personalization), id: 'preview-cta', class: 'btn-preview-book' %>";
linksDiv.html(htmlLinks);
